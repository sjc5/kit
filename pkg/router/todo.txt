validation on registration -- make sure patterns and trie as a whole are good?
no two patterns with competing dynamic segments at same index?
no single pattern with multiple same-named dynamic segments?
etc?

------------------------------------------------------------------------------------------

"INSTRUCTIONS"

SET CLIENT ERROR MESSAGES
SET HEADERS
SET COOKIES
SEND REDIRECTS
SEND CLIENT REDIRECT URLS
SET HEAD BLOCKS

------------------------------------------------------------------------------------------

Need more flexibility for Task Handlers generally in how to return http errors, statuses, etc.

And the nested version needs to consider it across parallel-run loaders, which means some sort
of copying-then-merging (and running "deepest" redirect, error status, etc.)

